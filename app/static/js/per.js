$(document).ready(function() {
    $('.sidenav').sidenav();
    $('.fixed-action-btn').floatingActionButton({
        toolbarEnabled: true
    });
    $('.slider').slider();
    $('.carousel').carousel();
});